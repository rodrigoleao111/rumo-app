package com.rodrigoleao.gramado2026.ui.theme

import androidx.compose.ui.graphics.Color

// ── Paleta Rumo ───────────────────────────────────────────────────────────────
// Valores alinhados ao board de identidade da marca (ver docs/brief-repaginacao-visual.md).
// O nome em português entre parênteses é o nome do board; o identificador em código é
// mantido estável porque é referenciado em toda a UI.

val GreenMoss       = Color(0xFF1B4332)   // Verde Musgo — primary: top bars, títulos, badges de data
val GreenSage       = Color(0xFF40916C)   // Verde Sálvia — secondary: datas, ações de suporte
val GreenLime       = Color(0xFFA7C957)   // Verde Claro — realces frescos, detalhes, tertiary
val GreenLight      = Color(0xFFFAF7F2)   // Creme — superfície clara alternativa (paleta; o fundo principal é Sand)
val GreenForest     = Color(0xFFE7EDE8)   // Cinza Claro — nav bar, superfícies suaves, cards de fundo
val GreenMist       = Color(0xFF2E3D34)   // Cinza Escuro — texto forte alternativo, superfícies escuras
val GreenWarm       = Color(0xFFC2D9CB)   // container suave esverdeado, hover states
val Sand            = Color(0xFFF4EDE1)   // Areia — background geral do app (fundo quente)
val GrayMuted       = Color(0xFFA8B5A9)   // Cinza Médio — texto auxiliar, ícones inativos

val AmberPrimary    = Color(0xFFE9B43C)   // Âmbar — accent / ação
val AmberLight      = Color(0xFFFDF3DD)   // fundo suave de badge/realce âmbar

val TextPrimary     = Color(0xFF0D1F16)   // quase-preto com tonalidade verde — títulos e texto principal
val TextSecondary   = Color(0xFF3A5045)   // verde-cinza escuro — subtítulos, labels, texto auxiliar

// Badges
val BadgeFreeText   = Color(0xFF1B4332)
val BadgeFreeBg     = Color(0x1A1B4332)
val BadgePaidText   = Color(0xFF7A5000)
val BadgePaidBg     = Color(0x1AE9B43C)
val BadgeBookedText = Color(0xFF1A3A8A)
val BadgeBookedBg   = Color(0x1A2850A0)
val BadgeUberText   = Color(0xFF444444)
val BadgeUberBg     = Color(0x0F000000)

val SurfaceWhite    = Color(0xFFFFFDF9)   // branco quente — superfície de cards (sobe acima do creme)
val CardBorder      = Color(0x501B4332)   // borda semitransparente do primary
val DividerColor    = Color(0x181B4332)   // divisória sutil
